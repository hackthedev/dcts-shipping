import{_ as a,o as t,c as i,ai as o}from"./chunks/framework.DsLHkhEs.js";const u=JSON.parse('{"title":"Technical overview","description":"","frontmatter":{},"headers":[],"relativePath":"Lore/Technical overview.md","filePath":"Lore/Technical overview.md"}'),n={name:"Lore/Technical overview.md"};function s(r,e,l,c,d,h){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="technical-overview" tabindex="-1">Technical overview <a class="header-anchor" href="#technical-overview" aria-label="Permalink to “Technical overview”">​</a></h1><p>DCTS is a system made of various sub systems that will be explained in this document. The purpose of this document is to shine some light on how things work. You may want to check out <a href="./Terminology">Terminology</a> first.</p><hr><h2 id="system-overview" tabindex="-1">System overview <a class="header-anchor" href="#system-overview" aria-label="Permalink to “System overview”">​</a></h2><p>Here is a short and simplified overview that will be further explained later in the document. To keep things simple for now it comes down to the following:</p><ul><li>Runtime: Bun/NodeJS <ul><li>Bun is preferred for performance reasons. NodeJS is used for legacy and can be used for bun-incompatible setups and systems like old hardware etc.</li></ul></li><li>Data: <ul><li>Most of the data is stored either in the database or <code>config.json</code> file. On launch specific data is pulled into memory and will continue to operate from there.</li></ul></li><li>DCTS Server <ul><li>The core that brings everything together and will hold all information. The web client is only the interface.</li></ul></li><li>Web Client <ul><li>Served and shipped with the DCTS Server. Communicates with the DCTS Server.</li></ul></li><li>Desktop Client <ul><li>An electron-based multi-os desktop client that enables private/public key based end-to-end encryption in DMs and server chat message signing. Basically an embedded Web Client with extra features.</li></ul></li><li>dSync <ul><li>Used for event-based server-to-server communication. It was custom purpose build to create decentralized networks and manage communications between servers/nodes, in that case between multiple instances/ DCTS Servers.</li></ul></li><li>MariaDB SQL Database <ul><li>Used for storing frequent data like messages, reports, audit logs and more.</li></ul></li><li>Livekit for Media sharing <ul><li>Responsible for handling voice calls, screen/camera sharing. Actual media like images etc are handled by the DCTS Server.</li></ul></li><li>Socket.io for real time socket events <ul><li>For example for messaging and other communication between the DCTS Server and web client.</li></ul></li></ul><hr><h2 id="auth" tabindex="-1">Auth <a class="header-anchor" href="#auth" aria-label="Permalink to “Auth”">​</a></h2><p>A member can register an account on any instance and that account will only be stored on that instance itself. It is possible for a member to export an account to re-import it on another instance to keep the same account, making accounts portable. Auth itself is usually done via a member&#39;s account id and token.</p><p>While currently not implemented, it is planned that this is automatically done with the desktop client in the future and in the background to keep things seamless. Its also planned to implement an &quot;account picker&quot; and the option to use default accounts.</p><p>Its also possible to authenticate using your private/public key by solving a challenge to verify that you actually are the owner of a public key thats linked to your account. While this is already implemented and working, its not being used yet. Accounts can only handle one public key as of right now as there is no update mechanism since its not fully implemented yet and are optional too.</p><p>In case you were logged out, you can re-import your account from the exported file to log back in. If you didnt export your account, you can also log-in traditionally by using your chosen login name and password.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>It is planned to automate auth with the desktop client soon.</p></div><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p>As of right now its not possible for you to change your password in case you forget it when trying to log-in. You will need to reach out to an instance admin for them to change it for you. This will soon be changed with an update too.</p></div><hr><h2 id="server-client-communication" tabindex="-1">Server / Client communication <a class="header-anchor" href="#server-client-communication" aria-label="Permalink to “Server / Client communication”">​</a></h2><p>The web client and DCTS server mostly communicate with each other using web sockets, specifically socket.io. There are some parts of the web client where communication will be done via simple http fetch requests.</p><div class="info custom-block github-alert"><p class="custom-block-title">INFO</p><p>Example flow:</p><p>User sends message » Web Client » Socket » Server » RAM Data » DB » Broadcast » Client</p></div><hr><h2 id="database-layer" tabindex="-1">Database Layer <a class="header-anchor" href="#database-layer" aria-label="Permalink to “Database Layer”">​</a></h2><p>The database is used to store frequent data like messages. It will also store user accounts, some specific cache, reports, dms, and much more. As of right now there are still some important parts that are stored inside the <code>config.json</code> file that will be moved to the database as well, like channels, groups, roles, role members and similar.</p><p>Its planned to move as much relevant info to the database to be able to load balance an instance or even have multiple instances running together to form some sort of CDN.</p><hr><h2 id="runtime-data" tabindex="-1">Runtime Data <a class="header-anchor" href="#runtime-data" aria-label="Permalink to “Runtime Data”">​</a></h2><p>When the instance starts, it will load relevant data from the <code>config.json</code> and database into ram and it keep it there. Whenever data is being updated it will change the data kept in ram and will at the same time update relevant files or the database. So currently both the <code>config.json</code> file and database are being used to run a DCTS server. Its planned to move more data to the database to enable &quot;multi-instance operations&quot;, as in having multiple processes or multiple servers from different regions operate on the same data.</p><p>This way operations are usually quick. So far there havent been any performance issues. More on the current performance can be found in <a href="./../Performance">Performance</a>.</p><hr><h2 id="media" tabindex="-1">Media <a class="header-anchor" href="#media" aria-label="Permalink to “Media”">​</a></h2><p>Its possible for members to upload data to an instance based on permissions, like &quot;can upload&quot; and &quot;upload limit&quot;. This means its possible to have individual per-role upload limits for members. Inside the <code>config.json</code> file is also a section for <code>uploadFileTypes</code> which is basically a whitelist of allowed file mime types.</p><p>On default these files are stored in <code>/public/uploads</code> which can be found in the instance&#39;s root path. Other media like voice calls, screen and camera shares are handled by livekit itself.</p><hr><h2 id="voip" tabindex="-1">VoIP <a class="header-anchor" href="#voip" aria-label="Permalink to “VoIP”">​</a></h2><p>For voice calls, screen and camera shares DCTS uses livekit. While it seems to support E2EE (end-to-end encryption) its currently not implemented. There are plans when VC Calls will be added for DMs to implement E2EE for these private calls.</p><hr><h2 id="decentralization" tabindex="-1">Decentralization <a class="header-anchor" href="#decentralization" aria-label="Permalink to “Decentralization”">​</a></h2><p>In order to form a flexible decentralized network <a href="https://www.npmjs.com/package/@hackthedev/dsync" target="_blank" rel="noreferrer">dSync</a> was created. It is a simple transport library to enable event based server-to-server communication, or in other words let nodes communicate with each other. It also comes with a few other custom-made libraries like <a href="https://www.npmjs.com/package/@hackthedev/dsync-sign" target="_blank" rel="noreferrer">dSyncSign</a> which takes care of event signing and verification and a few other libraries. In addition it will work based on a trust system for nodes and automatic penalties.</p><p>Generally speaking it is a very simple, flexible yet powerful family of libraries to take care about secure decentralized communication and will be a key component when it comes to federating specific data and powers the decentralized instance list inside DCTS and much more soon.</p><p>There are additional features like rate limits to prevent abuse and based on the implementation events can be emitted to broadcast abuse to other nodes to rule out bad actors automatically. Since all events must be signed, an event from an abuser can be shared securely and every node for themselves can verify the data from an event.</p><p>Connections between nodes wont be persistent and will only stay alive during data exchanges. It is designed to connect to the node directly, so its basically a peer-to-peer connection. It is required for dSync to implement a custom discovery mechanism, which is already implemented in DCTS and pretty trivial to do as well.</p><p>Automatic discovery is assisted by the desktop client, federation itself will not depend on it. It will store the instances it has connected to and will exchange that information when it connects to a node. When a user visits multiple servers it will automatically spread. Imagine it like a bee in a field of flowers. In addition its possible to manually submit nodes using the web client. Admins have a small status system to either block, approve or set a submitted node as pending.</p><p>Generally speaking, the dSync syntax and style is somewhat inspired from socket.io.</p>',41)])])}const p=a(n,[["render",s]]);export{u as __pageData,p as default};
