import{_ as i,o as t,c as s,ai as o,an as a,ao as n,ap as l}from"./chunks/framework.DsLHkhEs.js";const f=JSON.parse('{"title":"VoIP Setup","description":"","frontmatter":{},"headers":[],"relativePath":"VoIP Setup.md","filePath":"VoIP Setup.md"}'),r={name:"VoIP Setup.md"};function p(c,e,d,h,u,k){return t(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="voip-setup" tabindex="-1">VoIP Setup <a class="header-anchor" href="#voip-setup" aria-label="Permalink to “VoIP Setup”">​</a></h1><p>At the time of writing the entire VoIP Code and Setup has been completely rewritten in order to improve on stability and scalability. Setting up VoIP requires a bit of initial work.</p><p>The following document is based on the setup of the official instance and works pretty flawless once setup.</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p></p><p>If you use linux and want to run DCTS on a &quot;proper&quot; server you could install and use Plesk on your server to have an easier time with managing sub domains and reverse proxy setups. Personally i&#39;d recommend it as i dont wanna manually deal with config files etc. Keep in mind plesk is not free tho. This is a personal recommendation.</p></div><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>Its easier to use two subdomains in total where one is used for DCTS itself and the other is used for LiveKit. Checkout <a href="./network/DNS Setup">DNS Setup</a>.</p></div><hr><h2 id="livekit-service-config-setup" tabindex="-1">LiveKit Service Config Setup <a class="header-anchor" href="#livekit-service-config-setup" aria-label="Permalink to “LiveKit Service Config Setup”">​</a></h2><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>LiveKit installation instructions can be found <a href="https://docs.livekit.io/transport/self-hosting/local/" target="_blank" rel="noreferrer">on their website</a>. Generally it comes down to the following:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sSL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://get.livekit.io</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bash</span></span></code></pre></div></div><p>After you have installed LiveKit you should find a livekit config file inside the DCTS folder in <code>/livekit/livekit.yaml</code> that looks like the screenshot below when setup correctly.</p><p>Its important that you set <code>enabled</code> to <code>true</code>, and change the <code>domain</code> value to the domain you&#39;re using and linking the TLS certificate files. Infos for how to setup the domain can be found in <a href="./network/DNS Setup">DNS Setup</a>. After you&#39;ve setup the config file you can exit your editor of choice and start livekit with the following flag:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">livekit-server</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /home/dcts/livekit/livekit.yaml</span></span></code></pre></div><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>The secret for livekit needs to be a string with a minimum length of 32 characters! Consider using something like <a href="https://www.random.org/strings/?num=4&amp;len=32&amp;digits=on&amp;upperalpha=on&amp;loweralpha=on&amp;unique=on&amp;format=html&amp;rnd=new" target="_blank" rel="noreferrer">random.org</a></p><p>An example path where caddy TLS certs may be stored can be found in <a href="./network/Caddy Setup">Caddy Setup</a>.</p></div><p><img src="'+a+'" alt=""></p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p></p><p>Turn and TLS is required if you want others to be able to connect to VoIP. Its used for both talking and screensharing.</p><p>The <code>key</code> shown at line 2 can be changed freely and needs to match with the <code>livekit</code> settings inside the <code>configs/config.json</code> file.</p></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p></p><p>You can try and find errors by viewing the livekit log using <code>sudo journalctl -u livekit -f</code>. Both the desktop and web client will show logs inside the Web Dev Console (CTRL + Shift + i).</p></div><hr><h2 id="livekit-dcts-config-setup" tabindex="-1">LiveKit DCTS Config Setup <a class="header-anchor" href="#livekit-dcts-config-setup" aria-label="Permalink to “LiveKit DCTS Config Setup”">​</a></h2><p>Inside the <code>config.json</code> file in found in the <code>configs/</code>, you will find a section for livekit. This is where you set it to enabled and enter the other relevant data found inside the <code>livekit.yaml</code> file from the previous step. Once everything is set VoIP and Screensharing should work flawlessly now.</p><p><img src="'+n+'" alt="image-20251108182459231"></p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p></p><p>Before editing <strong>anything</strong> inside the config file manually, makes sure to stop the server/livekit, as your changes may get lost.</p><p>Also make sure that the <code>/livekit/livekit.yaml</code> and <code>/configs/config.json</code> settings for livekit are matching up.</p></div><hr><h2 id="livekit-reverse-proxy-setup" tabindex="-1">LiveKit Reverse Proxy Setup <a class="header-anchor" href="#livekit-reverse-proxy-setup" aria-label="Permalink to “LiveKit Reverse Proxy Setup”">​</a></h2><p>You can find the example proxy configs for livekit in [[network/Reverse Proxy Setup|Reverse Proxy Setup]].</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p></p><p>You can verify if livekit works by entering your subdomain in a browser and check if you&#39;ll see <code>OK</code> like in the screenshot below, given you&#39;ve already installed livekit and its running.</p></div><p><img src="'+l+'" alt="image-20251108182904477"></p>',25)])])}const m=i(r,[["render",p]]);export{f as __pageData,m as default};
